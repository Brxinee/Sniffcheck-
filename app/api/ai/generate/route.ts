import { NextResponse } from "next/server";
const categories = ["Gym","Office","Travel","College","Shoes","Helmet","Monsoon","Sweat","Confidence","Body Odor","Fabric Odor"];
export async function POST(req:Request){const {category="Confidence"}=await req.json().catch(()=>({})); if(!categories.includes(category))return NextResponse.json({error:"Unsupported category"},{status:400}); const prompt=`Create a Telegram post for Smelloff about ${category}. Return JSON with headline, emoji, body, cta, hashtags.`; if(process.env.OPENAI_API_KEY){const { default: OpenAI } = await import("openai"); const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY}); const completion=await client.chat.completions.create({model:"gpt-4o-mini",response_format:{type:"json_object"},messages:[{role:"system",content:"You write punchy odor-control Telegram posts."},{role:"user",content:prompt}]}); return NextResponse.json(JSON.parse(completion.choices[0]?.message.content||"{}"));} return NextResponse.json({headline:`Stay fresh after ${category.toLowerCase()}`,emoji:"🌿",body:"Beat odor before it steals your confidence. Keep Smelloff handy and walk into every moment feeling clean, fresh, and ready.",cta:"Try Smelloff today and share it with someone who needs a freshness upgrade.",hashtags:["#Smelloff","#BodyOdor","#StayFresh",`#${String(category).replaceAll(" ","")}`]});}
